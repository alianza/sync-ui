// export { auth as middleware } from "@/auth"; // import the auth middleware from the auth module,
// not possible for now due to mongoose not supporting edge runtime, and middleware only works in edge runtime
// possible solution, user prismaOrm instead of mongoose to connect to the database instead here
